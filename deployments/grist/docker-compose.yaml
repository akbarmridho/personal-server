services:
  grist:
    image: gristlabs/grist-oss:1.7.5
    container_name: grist
    ports:
      - "8016:8484"
    env_file:
      - .env
    volumes:
      - grist:/persist
    networks:
      - personal_network
    restart: unless-stopped

volumes:
  grist:

networks:
  personal_network:
    external: true
