# 3.14 Participant-Type Flow Breakdown

Status: DONE
Priority: P3
Effort: Small

## Change

Aggregate visible broker flow by participant type using the `broker_type` field already present in `fetch-broker-flow` output (`Asing` → foreign, `Pemerintah` → government, `Lokal` → local).

No mapping file or maintenance needed — the classification comes from the data source.

Output contract:

```yaml
participant_flow:
  foreign_net: -57430744500.0
  government_net: 15660306000.0
  local_net: 91550456000.0
  unclassified_net: 0.0
  dominant_type: local
  classified_pct: 1.0
```

## Files Modified

- `.opencode-config/skills/flow-analysis/scripts/build_flow_context.py` (passthrough `broker_type` in `_normalize_rows`, new `_participant_flow` aggregation, emit in output)
- `.opencode-config/skills/flow-analysis/SKILL.md` (interpretation rules, output contract)

## Acceptance

- Flow context emits `participant_flow` section with net flow per type
- `classified_pct` indicates coverage quality
- No external mapping file required

Detail: `docs/flow-future-improvements.md` §13
