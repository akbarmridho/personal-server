# 2.2 Flow Skill: Conviction Score Output

Status: PLANNED
Priority: Iteration 2
Effort: Small
Depends on: 2.0 validated

## Change

Replace flow's categorical verdict (ACCUMULATION/NEUTRAL/DISTRIBUTION) with a conviction score (0-100) plus structured evidence. The deterministic `flow_context.json` already has `conviction_pct` — normalize and expose it as the primary output.

Note: this supersedes 3.1 (strong verdict tiers). Once flow produces a continuous 0-100 score, the 5-tier categorical verdict is unnecessary — the score captures the same granularity continuously. If 3.1 has already shipped before this task, the 5-tier labels become a derived classification from the score rather than the primary output.

Output contract:

```yaml
flow_assessment:
  conviction_score: 45
  confidence: LOW
  bull_factors: [...]
  bear_factors: [...]
  sponsor_quality: {...}
  timing_context: {...}
  trust_regime: {...}
```

Keep all deterministic scripts, flow_context pipeline, and analytical frameworks unchanged.

## Files to Modify

- `.opencode-config/skills/flow-analysis/SKILL.md` (output contract + scoring rubric)

## Acceptance

- Flow produces a 0-100 conviction score with structured evidence
- Categorical verdict is either removed or derived from the score as a convenience label
